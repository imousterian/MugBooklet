<%= form_for(current_user.friendships.build(friend_id: available_friend.id), remote: true) do |f| %>
    <div><%= f.hidden_field :friend_id %></div>
    <%= f.submit "Request", class: "btn btn-xs btn-primary" %>
<% end %>
